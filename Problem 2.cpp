#include <bits/stdc++.h>
using namespace std;

int main(int argc, char *argv[]) {
    char a;
    cin>>a;

    if(a=='a' || a=='e' || a=='i' || a=='o' || a=='u' || a=='A' || a=='E' || a=='I' || a=='O' || a=='U')
    cout<<("母音\n");
    else
    cout<<("子音\n");
    
	return 0;
}
