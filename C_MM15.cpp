#include <bits/stdc++.h>
using namespace std;

int main(int argc, char *argv[]) {
    int x,y;
    cin>>x>>y;

    if(x<=100 && x>=0 && y>=0 && y<=100)
    cout<<"inside\n";
    else
cout<<"outside\n";
	return 0;
}
